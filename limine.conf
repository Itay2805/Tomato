# Boot straight into the kernel, there is nothing else to choose from
timeout: 0

/Tomato
    protocol: limine

    # boot():/ is the partition this config was found on
    path: boot():/boot/kernel.elf
